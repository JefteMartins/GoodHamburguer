name: CI

on:
  push:
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore GoodHamburguer.slnx

      - name: Restore local tools
        run: dotnet tool restore

      - name: Build
        run: dotnet build GoodHamburguer.slnx --no-restore -v minimal -p:TreatWarningsAsErrors=true

      - name: Validate Docker Compose
        run: docker compose -f docker/docker-compose.yml config --quiet

      - name: Unit tests with coverage
        run: dotnet test tests/GoodHamburguer.UnitTests/GoodHamburguer.UnitTests.csproj --no-build --collect:"XPlat Code Coverage" --results-directory artifacts/test-results/unit --logger:"GitHubActions;summary=true" -v minimal

      - name: Integration tests with coverage
        run: dotnet test tests/GoodHamburguer.IntegrationTests/GoodHamburguer.IntegrationTests.csproj --no-build --collect:"XPlat Code Coverage" --results-directory artifacts/test-results/integration --logger:"GitHubActions;summary=true" -v minimal

      - name: Blazor tests with coverage
        run: dotnet test tests/GoodHamburguer.Blazor.Tests/GoodHamburguer.Blazor.Tests.csproj --no-build --collect:"XPlat Code Coverage" --results-directory artifacts/test-results/blazor --logger:"GitHubActions;summary=true" -v minimal

      - name: Generate coverage report
        run: dotnet tool run reportgenerator -reports:"artifacts/test-results/**/coverage.cobertura.xml" -targetdir:"artifacts/coverage-report" -reporttypes:"TextSummary;HtmlInline_AzurePipelines;MarkdownSummaryGitHub"

      - name: Enforce coverage threshold
        run: |
          summary_file="artifacts/coverage-report/Summary.txt"
          echo "Coverage summary:"
          cat "$summary_file"
          line_rate=$(grep "Line coverage" "$summary_file" | sed -E 's/.*: ([0-9]+\.[0-9]+)%.*/\1/')
          awk -v rate="$line_rate" 'BEGIN { exit !(rate >= 70.0) }'

      - name: Upload test and coverage artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-and-coverage-results
          path: |
            artifacts/test-results
            artifacts/coverage-report
